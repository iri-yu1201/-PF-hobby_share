<% if current_user.already_favo?(item) %>
  <%= link_to "いいねを外す", item_favorites_path(item), method: :delete, remote: true %>
<% else %>
  <%= link_to "いいね", item_favorites_path(item), method: :post, remote: true %>
<% end %>
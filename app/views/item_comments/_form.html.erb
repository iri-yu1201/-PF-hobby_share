<%= form_with(model: [item, item_comment], url: item_item_comments_path(item)) do |f| %>
<div class="row">
  <%= f.text_area :comment, size: "50×10" %>
  <%= f.submit "コメントする", class: "btn btn-success" %>
</div>
<% end %>